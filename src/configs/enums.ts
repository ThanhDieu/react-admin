export enum ELoad {
  INIT = 'init',
  PENDING = 'pending',
  FULFILLED = 'fulfilled',
  REJECTED = 'rejected'
}

export enum EFuncType {
  CREATE = 'created',
  READ = 'read',
  UPDATE = 'updated',
  DELETE = 'deleted'
}
